externalId: "tr_sap_equipment_{{location}}_to_asset"
dataSetExternalId: "{{dataset}}"
name: "SAP Equipment → ISAAsset relation ({{location}})"
destination:
  type: nodes
  view:
    space: "{{isaSchemaSpace}}"
    externalId: Equipment
    version: "{{viewVersion}}"
  instanceSpace: "{{instanceSpace}}"
conflictMode: upsert
isPublic: true
ignoreNullFields: true
