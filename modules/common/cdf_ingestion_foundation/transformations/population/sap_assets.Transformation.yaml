externalId: "tr_sap_assets_{{location}}_to_isa"
dataSetExternalId: "{{dataset}}"
name: "SAP Functional Locations → ISAAsset ({{location}})"
destination:
  type: nodes
  view:
    space: "{{isaSchemaSpace}}"
    externalId: ISAAsset
    version: "{{viewVersion}}"
  instanceSpace: "{{instanceSpace}}"
conflictMode: upsert
isPublic: true
ignoreNullFields: true
