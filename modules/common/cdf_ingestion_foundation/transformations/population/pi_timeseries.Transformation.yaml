externalId: "tr_pi_timeseries_{{location}}_to_isa"
dataSetExternalId: "{{dataset}}"
name: "PI TimeSeries → ISATimeSeries ({{location}})"
destination:
  type: nodes
  view:
    space: "{{isaSchemaSpace}}"
    externalId: ISATimeSeries
    version: "{{viewVersion}}"
  instanceSpace: "{{instanceSpace}}"
conflictMode: upsert
isPublic: true
ignoreNullFields: true
