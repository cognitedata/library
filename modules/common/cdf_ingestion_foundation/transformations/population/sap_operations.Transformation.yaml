externalId: "tr_sap_operations_{{location}}_to_isa"
dataSetExternalId: "{{dataset}}"
name: "SAP Notifications → Operation ({{location}})"
destination:
  type: nodes
  view:
    space: "{{isaSchemaSpace}}"
    externalId: Operation
    version: "{{viewVersion}}"
  instanceSpace: "{{instanceSpace}}"
conflictMode: upsert
isPublic: true
ignoreNullFields: true
