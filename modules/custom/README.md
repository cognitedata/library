# Custom modules

`my_module` is an **empty template** for the `dp:emptymodule` deployment pack. Copy it as a starting point for project-specific Toolkit modules; it is not a maintained product module.
