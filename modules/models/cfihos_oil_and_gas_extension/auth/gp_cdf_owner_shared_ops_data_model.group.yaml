name: gp_{{environment}}_owner_shared_ops_data_model
sourceId: {{owner_source_id}}
capabilities:
  - datasetsAcl:
      actions:
        - OWNER
        - READ
        - WRITE
      scope:
        idScope:
          ids:
            - {{dataset_external_id}}
  - dataModelsAcl:
      actions:
        - READ
        - WRITE
      scope:
        spaceIdScope:
          spaceIds:
            - {{space}}
  - dataModelsAcl:
      actions:
        - READ
      scope:
        spaceIdScope:
          spaceIds:
            - cdf_cdm
  - dataModelInstancesAcl:
      actions:
        - READ
      scope:
        spaceIdScope:
          spaceIds:
            - cdf_cdm
metadata:
  origin: {{admin_user}}
