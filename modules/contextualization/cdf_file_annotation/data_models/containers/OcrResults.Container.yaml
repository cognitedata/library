space: {{ contextualizationExtensionsSchemaSpace }}
externalId: {{ ocrContainerExternalId }}
name: File OCR Results
description: Container that holds information on the results of the OCR endpoint for a file
properties:
  ocrResults:
    type:
      list: false
      type: json
    immutable: false
    nullable: true
    autoIncrement: false
    name: results
constraints:
  requireDiagramDetect:
    constraintType: requires
    require:
      space: {{ contextualizationExtensionsSchemaSpace }}
      externalId: {{ diagramDetectContainerExternalId }}
