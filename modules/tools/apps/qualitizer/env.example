## UI (Vite) configuration
# Enable standalone mode (skip Dune host integration)
VITE_STANDALONE=true

# CDF project name
CDF_PROJECT=your-cdf-project

# Proxy base URL (when running standalone)
CDF_PROXY_URL=http://localhost:4243

# Optional: CDF base URL (displayed in UI)
CDF_URL=https://api.cognitedata.com

# Optional: CDF app id for standalone
VITE_CDF_APP_ID=qualitizer-standalone

# Optional: base URL for SDK when not using proxy
VITE_CDF_BASE_URL=https://api.cognitedata.com

# Optional: client id / scopes forwarded to the UI
IDP_CLIENT_ID=your-client-id
IDP_SCOPES=https://api.cognitedata.com/.default

## Proxy configuration (server/proxy.mjs)
# Required for proxy token exchange
IDP_TOKEN_URL=https://login.microsoftonline.com/<tenant>/oauth2/v2.0/token
IDP_CLIENT_SECRET=your-client-secret

# Since this will only run localhost in a trusted environment, we can be unsafe to avoid lots of certificate problems.
PROXY_INSECURE=true