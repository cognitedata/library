# No user-configurable variables required for this module.
# Sample data uses the same fixed spaces as the rest of the Maintain CDM quickstart.
# Disable this module in production deployments — it is for demo/testing purposes only.
