name: 'applications-configuration'
sourceId: '{{applicationsconfiguration_source_id}}'
metadata:
  origin: 'cdf-project-templates'
capabilities:

  # Groups - For InField administrators to grant access to users
  - groupsAcl:
      actions:
        - LIST
        - READ
      scope:
        all: {}

  # 3D - Upload 3D models to display in InField
  - threedAcl:
      actions:
        - READ
      scope:
        all: {}

  # Assets - View asset data from the CDF project that InField runs on top of
  - assetsAcl:
      actions:
        - READ
      scope:
        all: {}

  # Data sets - Get data from work order management system, such as SAP, and work orders
  - datasetsAcl:
      actions:
        - READ
      scope:
        all: {}

  # Data models - Organize and structure data
  - dataModelsAcl:
      actions:
        - READ
      scope:
        all: {}

  # Data model instances - Access data organized in data models (read)
  - dataModelInstancesAcl:
      actions:
        - READ
      scope:
        all: {}

  # Data model instances - Access and edit data organized in data models and spaces (write)
  - dataModelInstancesAcl:
      actions:
        - WRITE
      scope:
        spaceIdScope:
          spaceIds:
            - cognite_app_data
            - APM_Config

  # Files - Allow users to upload images
  - filesAcl:
      actions:
        - READ
        - WRITE
      scope:
        all: {}

  # Time series - Allow users to upload measurement readings
  - timeSeriesAcl:
      actions:
        - READ
        - WRITE
      scope:
        all: {}

  # Location filters - Configure location filters (admin feature)
  - locationFiltersAcl:
      actions:
        - READ
        - WRITE
      scope:
        all: {}