# Generated by scripts/build_scopes.py (workflow_definitions_scoped). # WorkflowVersion from compiled_workflow IR; created if missing; pass --force to overwrite.
# Scope: /Users/darren.downtain@cognitedata.com/Documents/GitHub/library/modules/accelerators/contextualization/cdf_key_extraction_aliasing/workflow_template/workflow.template.config.yaml
workflowExternalId: key_extraction_aliasing.site_02_plant_a_area_01_cooling_sys
version: v5
workflowDefinition:
  description: Canvas-driven key extraction and aliasing (node-per-task). Each task
    passes task_id + compiled_workflow in function data.
  input:
    run_all: false
    run_id: ''
    configuration: {}
    compiled_workflow: {}
  tasks:
  - externalId: kea__incremental_state
    type: function
    parameters:
      function:
        externalId: fn_dm_incremental_state_update
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__incremental_state
    name: Incremental state (cohort)
    description: Detect changed CDM instances per source_views and write cohort rows
      to RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
  - externalId: kea__ext_asset_tag_candidate
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__ext_asset_tag_candidate
    name: Key extraction
    description: Extract candidate keys and FK/document references; write key-extraction
      RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_equipment_tag_ck
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__ext_equipment_tag_ck
    name: Key extraction
    description: Extract candidate keys and FK/document references; write key-extraction
      RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_instance_description_asset_fk
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__ext_instance_description_asset_fk
    name: Key extraction
    description: Extract candidate keys and FK/document references; write key-extraction
      RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_instance_name_asset_fk
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__ext_instance_name_asset_fk
    name: Key extraction
    description: Extract candidate keys and FK/document references; write key-extraction
      RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_timeseries_instrument_tag_candidate
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__ext_timeseries_instrument_tag_candidate
    name: Key extraction
    description: Extract candidate keys and FK/document references; write key-extraction
      RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__al_cogniteasset_explicit_aliases_from_raw
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__al_cogniteasset_explicit_aliases_from_raw
    name: Aliasing
    description: Apply aliasing rules to extracted keys; write tag-aliasing RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_asset_tag_candidate
  - externalId: kea__al_leading_zero_normalization
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__al_leading_zero_normalization
    name: Aliasing
    description: Apply aliasing rules to extracted keys; write tag-aliasing RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_asset_tag_candidate
  - externalId: kea__al_semantic_expansion
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__al_semantic_expansion
    name: Aliasing
    description: Apply aliasing rules to extracted keys; write tag-aliasing RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_asset_tag_candidate
  - externalId: kea__al_strip_delimiter
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__al_strip_delimiter
    name: Aliasing
    description: Apply aliasing rules to extracted keys; write tag-aliasing RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__al_cogniteasset_explicit_aliases_from_raw
    - externalId: kea__al_leading_zero_normalization
    - externalId: kea__al_semantic_expansion
    - externalId: kea__al_strip_numeric_unit_prefix
  - externalId: kea__al_strip_numeric_unit_prefix
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__al_strip_numeric_unit_prefix
    name: Aliasing
    description: Apply aliasing rules to extracted keys; write tag-aliasing RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_asset_tag_candidate
  - externalId: kea__al_document_aliases
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__al_document_aliases
    name: Aliasing
    description: Apply aliasing rules to extracted keys; write tag-aliasing RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_file_basename_candidate
  - externalId: kea__ext_file_basename_candidate
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__ext_file_basename_candidate
    name: Key extraction
    description: Extract candidate keys and FK/document references; write key-extraction
      RAW.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__n_1776968044470_89wvovc3
    type: function
    parameters:
      function:
        externalId: fn_dm_reference_index
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__n_1776968044470_89wvovc3
    name: Reference index
    description: Build RAW inverted index for FK and document references.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_equipment_tag_ck
    - externalId: kea__ext_instance_description_asset_fk
    - externalId: kea__ext_instance_name_asset_fk
    - externalId: kea__ext_timeseries_instrument_tag_candidate
  - externalId: kea__n_1776968047289_da4zzh3o
    type: function
    parameters:
      function:
        externalId: fn_dm_alias_persistence
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          compiled_workflow: ${workflow.input.compiled_workflow}
          task_id: kea__n_1776968047289_da4zzh3o
    name: Alias persistence
    description: Read aliasing RAW and write aliases onto source instances.
    retries: 3
    timeout: 3600
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__al_document_aliases
    - externalId: kea__al_strip_delimiter
