# Generated by scripts/build_scopes.py (workflow_definitions_scoped). # Created if missing; pass --force to overwrite from the workflow template.
# Workflow template: workflow_template/workflow.template.Workflow.yaml
# Template: key_extraction_aliasing.site_02_plant_a_area_01_cooling_sys → scoped workflow external id (base.suffix per leaf).
externalId: key_extraction_aliasing.site_02_plant_a_area_01_cooling_sys
name: Second Manufacturing Site - Cooling System
description: >-
  Key extraction, optional RAW reference index, tag aliasing, and alias write-back to CDM describables.
  Source views, extraction rules, and aliasing rules come from workflow.input.configuration (see workflow_template/workflow.template.config.yaml).
dataSetExternalId: 'ds_key_extraction'
