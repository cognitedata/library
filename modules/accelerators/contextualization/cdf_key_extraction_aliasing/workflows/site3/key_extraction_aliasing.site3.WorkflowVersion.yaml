# Generated by scripts/build_scopes.py (workflow_definitions_scoped). # WorkflowVersion from build-time canvas IR (per-task fields inlined); pass --force to overwrite.
# Scope: /Users/darren.downtain@cognitedata.com/Documents/GitHub/library/modules/accelerators/contextualization/cdf_key_extraction_aliasing/workflow_template/workflow.template.config.yaml
workflowExternalId: key_extraction_aliasing.site3
version: v5
workflowDefinition:
  description: Canvas-driven key extraction and aliasing (node-per-task). Each task
    passes task_id plus inlined payload/persistence from build-time IR; workflow.input
    carries configuration only.
  input:
    run_all: false
    run_id: ''
    configuration: {}
  tasks:
  - externalId: kea__incremental_state
    type: function
    parameters:
      function:
        externalId: fn_dm_incremental_state_update
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__incremental_state
          pipeline_node_id: kea__incremental_state
    name: Incremental state (cohort)
    description: Detect changed CDM instances per source_views and write cohort rows
      to RAW (RUN_ID, WORKFLOW_STATUS), or no-op when incremental_change_processing
      is false (stable DAG).
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
  - externalId: kea__al_cogniteasset_explicit_aliases_from_raw
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__al_cogniteasset_explicit_aliases_from_raw
          pipeline_node_id: kea__al_cogniteasset_explicit_aliases_from_raw
          canvas_node_id: al_cogniteasset_explicit_aliases_from_raw
          aliasing_rule_names:
          - cogniteasset_explicit_aliases_from_raw
          source_raw_db: db_key_extraction
          source_raw_read_limit: 10000
          incremental_auto_run_id: true
          incremental_transition: true
          source_view_space: cdf_cdm
          source_view_external_id: CogniteFile
          source_view_version: v1
          source_entity_type: file
    name: Aliasing
    description: 'Apply aliasing_rules to extracted keys: read key-extraction RAW,
      run AliasingEngine, write tag-aliasing RAW.'
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_asset_tag_candidate
    - externalId: kea__ext_equipment_tag_ck
  - externalId: kea__al_document_aliases
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__al_document_aliases
          pipeline_node_id: kea__al_document_aliases
          canvas_node_id: al_document_aliases
          aliasing_rule_names:
          - document_aliases
          source_raw_db: db_key_extraction
          source_raw_read_limit: 10000
          incremental_auto_run_id: true
          incremental_transition: true
          source_view_space: cdf_cdm
          source_view_external_id: CogniteFile
          source_view_version: v1
          source_entity_type: file
    name: Aliasing
    description: 'Apply aliasing_rules to extracted keys: read key-extraction RAW,
      run AliasingEngine, write tag-aliasing RAW.'
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_file_basename_candidate
  - externalId: kea__al_leading_zero_normalization
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__al_leading_zero_normalization
          pipeline_node_id: kea__al_leading_zero_normalization
          canvas_node_id: al_leading_zero_normalization
          aliasing_rule_names:
          - leading_zero_normalization
          source_raw_db: db_key_extraction
          source_raw_read_limit: 10000
          incremental_auto_run_id: true
          incremental_transition: true
          source_view_space: cdf_cdm
          source_view_external_id: CogniteFile
          source_view_version: v1
          source_entity_type: file
    name: Aliasing
    description: 'Apply aliasing_rules to extracted keys: read key-extraction RAW,
      run AliasingEngine, write tag-aliasing RAW.'
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_asset_tag_candidate
  - externalId: kea__al_strip_delimiter
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__al_strip_delimiter
          pipeline_node_id: kea__al_strip_delimiter
          canvas_node_id: al_strip_delimiter
          aliasing_rule_names:
          - Strip Delimiter
          source_raw_db: db_key_extraction
          source_raw_read_limit: 10000
          incremental_auto_run_id: true
          incremental_transition: true
          source_view_space: cdf_cdm
          source_view_external_id: CogniteFile
          source_view_version: v1
          source_entity_type: file
    name: Aliasing
    description: 'Apply aliasing_rules to extracted keys: read key-extraction RAW,
      run AliasingEngine, write tag-aliasing RAW.'
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__al_cogniteasset_explicit_aliases_from_raw
    - externalId: kea__al_leading_zero_normalization
    - externalId: kea__al_strip_numeric_unit_prefix
  - externalId: kea__al_strip_numeric_unit_prefix
    type: function
    parameters:
      function:
        externalId: fn_dm_aliasing
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__al_strip_numeric_unit_prefix
          pipeline_node_id: kea__al_strip_numeric_unit_prefix
          canvas_node_id: al_strip_numeric_unit_prefix
          aliasing_rule_names:
          - strip_numeric_unit_prefix
          source_raw_db: db_key_extraction
          source_raw_read_limit: 10000
          incremental_auto_run_id: true
          incremental_transition: true
          source_view_space: cdf_cdm
          source_view_external_id: CogniteFile
          source_view_version: v1
          source_entity_type: file
    name: Aliasing
    description: 'Apply aliasing_rules to extracted keys: read key-extraction RAW,
      run AliasingEngine, write tag-aliasing RAW.'
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__ext_asset_tag_candidate
  - externalId: kea__ext_asset_tag_candidate
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__ext_asset_tag_candidate
          pipeline_node_id: kea__ext_asset_tag_candidate
          canvas_node_id: ext_asset_tag_candidate
          extraction_rule_names:
          - asset_tag_candidate
    name: Key extraction
    description: Extract candidate keys and foreign-key references from assets, files,
      and timeseries; write key-extraction RAW rows.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_equipment_tag_ck
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__ext_equipment_tag_ck
          pipeline_node_id: kea__ext_equipment_tag_ck
          canvas_node_id: ext_equipment_tag_ck
          extraction_rule_names:
          - equipment_tag_ck
    name: Key extraction
    description: Extract candidate keys and foreign-key references from assets, files,
      and timeseries; write key-extraction RAW rows.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_file_basename_candidate
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__ext_file_basename_candidate
          pipeline_node_id: kea__ext_file_basename_candidate
          canvas_node_id: ext_file_basename_candidate
          extraction_rule_names:
          - file_basename_candidate
    name: Key extraction
    description: Extract candidate keys and foreign-key references from assets, files,
      and timeseries; write key-extraction RAW rows.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_instance_description_asset_fk
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__ext_instance_description_asset_fk
          pipeline_node_id: kea__ext_instance_description_asset_fk
          canvas_node_id: ext_instance_description_asset_fk
          extraction_rule_names:
          - instance_description_asset_fk
    name: Key extraction
    description: Extract candidate keys and foreign-key references from assets, files,
      and timeseries; write key-extraction RAW rows.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_instance_name_asset_fk
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__ext_instance_name_asset_fk
          pipeline_node_id: kea__ext_instance_name_asset_fk
          canvas_node_id: ext_instance_name_asset_fk
          extraction_rule_names:
          - instance_name_asset_fk
    name: Key extraction
    description: Extract candidate keys and foreign-key references from assets, files,
      and timeseries; write key-extraction RAW rows.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
  - externalId: kea__ext_timeseries_instrument_tag_candidate
    type: function
    parameters:
      function:
        externalId: fn_dm_key_extraction
        data:
          logLevel: INFO
          run_all: ${workflow.input.run_all}
          configuration: ${workflow.input.configuration}
          task_id: kea__ext_timeseries_instrument_tag_candidate
          pipeline_node_id: kea__ext_timeseries_instrument_tag_candidate
          canvas_node_id: ext_timeseries_instrument_tag_candidate
          extraction_rule_names:
          - timeseries_instrument_tag_candidate
    name: Key extraction
    description: Extract candidate keys and foreign-key references from assets, files,
      and timeseries; write key-extraction RAW rows.
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
    dependsOn:
    - externalId: kea__incremental_state
