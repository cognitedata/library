# Generated by scripts/build_scopes.py (workflow_definitions_scoped). # --build creates these only if missing (--force overwrites from templates).
# Workflow template: workflow_template/workflow.template.Workflow.yaml
# WorkflowVersion template: workflow_template/workflow.template.WorkflowVersion.yaml
# Template: key_extraction_aliasing.site_01_plant_a_area_01_cooling_sys → workflow external id (base or base.suffix). See default.config.yaml scope_build_mode.
externalId: key_extraction_aliasing.site_01_plant_a_area_01_cooling_sys
description: Key extraction, aliasing, and alias persistence (scope via workflow input from triggers)
dataSetExternalId: 'ds_key_extraction'
