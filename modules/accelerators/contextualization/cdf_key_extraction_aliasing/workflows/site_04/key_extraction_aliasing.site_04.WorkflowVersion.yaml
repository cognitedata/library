# Generated by scripts/build_scopes.py (workflow_definitions_scoped). # --build creates these only if missing (--force overwrites from templates).
# Workflow template: workflow_template/workflow.template.Workflow.yaml
# WorkflowVersion template: workflow_template/workflow.template.WorkflowVersion.yaml
# v4: Scope rules live in workflow.input.configuration (v1 mapping). Each function receives it via
# parameters.function.data; no Cognite File upload for scope config. instance_space is derived from
# that object (source_views instance_space or node space filter) when not passed on task data.
# Optional run_id, full_rescan on workflow.input. Authoring: workflow_template/
# workflow.template.config.yaml + scripts/build_scopes.py.
# Template: key_extraction_aliasing.site_04 → must match sibling Workflow externalId.
workflowExternalId: key_extraction_aliasing.site_04
version: 'v4'
workflowDefinition:
  description: 'Run key extraction, aliasing, and alias persistence (config from workflow.input.configuration)'
  input:
    full_rescan: false
    run_id: ''
    configuration: {}
  tasks:
    - externalId: 'fn_dm_incremental_state_update'
      type: 'function'
      parameters:
        function:
          externalId: 'fn_dm_incremental_state_update'
          data:
            logLevel: 'INFO'
            full_rescan: ${workflow.input.full_rescan}
            configuration: ${workflow.input.configuration}
      name: 'Incremental state (cohort)'
      description: 'Detect changes and write cohort rows to RAW (WORKFLOW_STATUS=detected)'
      retries: 3
      timeout: 7200
      onFailure: 'abortWorkflow'

    - externalId: 'fn_dm_key_extraction'
      type: 'function'
      parameters:
        function:
          externalId: 'fn_dm_key_extraction'
          data:
            logLevel: 'INFO'
            full_rescan: ${workflow.input.full_rescan}
            configuration: ${workflow.input.configuration}
      name: 'Key extraction'
      description: 'Extract candidate keys from scoped files and write to RAW'
      retries: 3
      timeout: 7200
      onFailure: 'abortWorkflow'
      dependsOn:
        - externalId: 'fn_dm_incremental_state_update'

    - externalId: 'fn_dm_reference_index'
      type: 'function'
      parameters:
        function:
          externalId: 'fn_dm_reference_index'
          data:
            logLevel: 'INFO'
            configuration: ${workflow.input.configuration}
            source_raw_db: db_key_extraction
            source_raw_read_limit: 10000
            incremental_auto_run_id: true
            reference_index_raw_db: db_key_extraction
            source_view_space: cdf_cdm
            source_view_external_id: CogniteFile
            source_view_version: v1
            reference_index_fk_entity_type: asset
            reference_index_document_entity_type: file
      name: 'Reference index'
      description: 'RAW inverted index for FK and document references (parallel to aliasing)'
      retries: 3
      timeout: 7200
      onFailure: 'abortWorkflow'
      dependsOn:
        - externalId: 'fn_dm_key_extraction'

    - externalId: 'fn_dm_aliasing'
      type: 'function'
      parameters:
        function:
          externalId: 'fn_dm_aliasing'
          data:
            logLevel: 'INFO'
            source_raw_db: db_key_extraction
            source_raw_read_limit: 10000
            incremental_auto_run_id: true
            incremental_transition: true
            source_view_space: cdf_cdm
            source_view_external_id: CogniteFile
            source_view_version: v1
            source_entity_type: file
            configuration: ${workflow.input.configuration}
      name: 'Aliasing'
      description: 'Generate aliases from extracted candidate keys and write aliasing results to RAW'
      retries: 3
      timeout: 7200
      onFailure: 'abortWorkflow'
      dependsOn:
        - externalId: 'fn_dm_key_extraction'

    - externalId: 'fn_dm_alias_persistence'
      type: 'function'
      parameters:
        function:
          externalId: 'fn_dm_alias_persistence'
          data:
            logLevel: 'INFO'
            raw_db: db_tag_aliasing
            raw_read_limit: 10000
            source_raw_db: db_key_extraction
            source_raw_read_limit: 10000
            incremental_auto_run_id: true
            incremental_transition: true
            configuration: ${workflow.input.configuration}
            source_view_space: cdf_cdm
            source_view_external_id: CogniteFile
            source_view_version: v1
            write_foreign_key_references: false
            foreign_key_writeback_property: references_found
      dependsOn:
        - externalId: 'fn_dm_aliasing'
      name: 'Alias persistence'
      description: 'Persist aliases to cdf_cdm:CogniteDescribable:v1'
      retries: 3
      timeout: 3600
      onFailure: 'abortWorkflow'
