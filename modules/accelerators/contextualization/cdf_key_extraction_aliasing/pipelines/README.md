# Pipelines (moved)

Pipeline YAML and related reference material now live under **`config/`**:

- **Production-style combined scopes:** `config/scopes/<scope>/key_extraction_aliasing.yaml`
- **Examples and legacy split configs:** `config/examples/` (including `*.config.yaml`, `.ExtractionPipeline.yaml`, and `LEGACY_TO_NEW_*.md`)

See [`config/README.md`](../config/README.md).
