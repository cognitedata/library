externalId: trg_wf_etl_clear_aliases
workflowExternalId: wf_etl_clear_aliases
workflowVersion: '1'
triggerRule:
  triggerType: schedule
  cronExpression: 0 2 * * *
input:
  incremental_change_processing: true
  configuration:
    schemaVersion: 1
    id: clear_aliases_template
    scope: null
    parameters:
      incremental_change_processing: true
      incremental_skip_unchanged: true
      raw_db: etl_staging
      raw_table_key: cohort
      preview_raw_table_key: etl_preview
      instance_space: inst_assets
      etl_state_instance_space: null
authentication:
  clientId: '{{ workflowClientId }}'
  clientSecret: '{{ workflowClientSecret }}'
