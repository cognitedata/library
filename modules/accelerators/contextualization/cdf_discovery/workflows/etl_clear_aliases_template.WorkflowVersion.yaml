workflowExternalId: wf_etl_clear_aliases
version: '1'
workflowDefinition:
  tasks:
  - externalId: query_view_mppwe49g
    type: function
    dependsOn: []
    parameters:
      function:
        externalId: fn_discovery_etl_view_query
        data:
          logLevel: INFO
          incremental_change_processing: ${workflow.input.incremental_change_processing}
          run_id: ${workflow.input.configuration.parameters.run_id}
          configuration: ${workflow.input.configuration}
          task_id: query_view_mppwe49g
          canvas_node_id: query_view_mppwe49g
          config:
            description: CogniteAsset (v1)
            view_space: cdf_cdm
            view_external_id: CogniteAsset
            view_version: v1
            batch_size: 1000
            include_properties:
            - name
          predecessors: []
    name: CogniteAsset (v1)
    description: Execute fn_discovery_etl_view_query
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
  - externalId: save_view_mppwdkbe
    type: function
    dependsOn:
    - externalId: transform_mppwfbt9
    parameters:
      function:
        externalId: fn_discovery_etl_view_save
        data:
          logLevel: INFO
          incremental_change_processing: ${workflow.input.incremental_change_processing}
          run_id: ${workflow.input.configuration.parameters.run_id}
          configuration: ${workflow.input.configuration}
          task_id: save_view_mppwdkbe
          canvas_node_id: save_view_mppwdkbe
          config:
            description: CogniteDescribable(v1)
            view_space: cdf_cdm
            view_external_id: CogniteDescribable
            view_version: v1
          predecessors:
          - transform_mppwfbt9
    name: CogniteAsset (v1)
    description: Execute fn_discovery_etl_view_save
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
  - externalId: transform_mppwfbt9
    type: function
    dependsOn:
    - externalId: query_view_mppwe49g
    parameters:
      function:
        externalId: fn_discovery_etl_transform
        data:
          logLevel: INFO
          incremental_change_processing: ${workflow.input.incremental_change_processing}
          run_id: ${workflow.input.configuration.parameters.run_id}
          configuration: ${workflow.input.configuration}
          task_id: transform_mppwfbt9
          canvas_node_id: transform_mppwfbt9
          config:
            handler_id: trim_whitespace
            enabled: true
            fields:
            - field_name: name
            output_field: aliases
            output_template: NONE
            output_mode: append
            trim_whitespace:
              mode: ends_only
            description: "Trim whitespace \xB7 CogniteAsset (v1)"
          predecessors:
          - query_view_mppwe49g
    name: "Trim whitespace \xB7 CogniteAsset (v1)"
    description: Execute fn_discovery_etl_transform
    retries: 3
    timeout: 7200
    onFailure: abortWorkflow
  - externalId: end
    type: function
    dependsOn:
    - externalId: save_view_mppwdkbe
    parameters:
      function:
        externalId: fn_discovery_etl_raw_cleanup
        data:
          logLevel: INFO
          incremental_change_processing: ${workflow.input.incremental_change_processing}
          run_id: ${workflow.input.configuration.parameters.run_id}
          configuration: ${workflow.input.configuration}
          task_id: end
          canvas_node_id: end
          config:
            description: ''
          predecessors:
          - save_view_mppwdkbe
    name: Workflow step
    description: Execute fn_discovery_etl_raw_cleanup
    retries: 3
    timeout: 1800
    onFailure: abortWorkflow
