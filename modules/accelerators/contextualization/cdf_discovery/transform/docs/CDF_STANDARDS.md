# CDF Discovery ETL — CDF standards

- Module id: `dp:acc:ctx:discovery_etl` (see `../module.toml`)
- Cognite Functions runtime: `py311`
- Function external ids: `fn_etl_*` (see `functions/functions.Function.yaml`)
- Dataset external id: `ds_discovery_etl`
- No runtime imports from `cdf_discovery_aliasing`
