name: gp_site_a_integration_reader
sourceId: "{{ gp_site_a_integration_reader }}"
capabilities:
  - dataModelInstancesAcl:
      scope:
        spaceIdScope:
          spaceIds:
            - inst_erp_site_a
            - inst_scada_site_a