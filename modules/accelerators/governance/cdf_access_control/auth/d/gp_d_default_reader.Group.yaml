name: gp_d_default_reader
sourceId: "{{ gp_d_default_reader }}"
capabilities:
  - dataModelInstancesAcl:
      scope:
        spaceIdScope:
          spaceIds:
            - inst_erp_d
            - inst_scada_d