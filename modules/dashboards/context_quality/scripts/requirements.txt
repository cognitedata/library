# Context Quality Metrics - Local Script Dependencies
#
# Install with: pip install -r requirements.txt

# Cognite SDK
cognite-sdk>=7.0.0

# Load .env files (cognite-toolkit compatible)
python-dotenv>=1.0.0

# Progress bars (optional but recommended)
tqdm>=4.65.0
