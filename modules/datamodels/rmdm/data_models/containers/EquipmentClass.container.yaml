space: rmdm
externalId: EquipmentClass
name: RM Equipment Class
description: Stores equipment class codes and related failure mode relationships.
properties:
  code:
    type:
      list: false
      collation: ucs_basic
      type: text
    immutable: false
    nullable: true
    autoIncrement: false
    name: Code
    description: A unique identifier for the class of asset.
  standard:
    type:
      list: false
      collation: ucs_basic
      type: text
    immutable: false
    nullable: true
    autoIncrement: false
    name: Standard
    description: A text string to specify which standard the class is from.
  failureModes:
    type:
      list: true
      maxListSize: 300
      type: direct
    immutable: false
    nullable: true
    autoIncrement: false
    name: Failure Modes
    description: The distinct ways in which an asset or component can fail.
constraints:
  cdf_cdm_CogniteDescribable:
    require:
      space: cdf_cdm
      externalId: CogniteDescribable
      type: container
    constraintType: requires
  uniqueCodeConstraint:
    properties:
    - failureModes
    - code
    - standard
    constraintType: uniqueness
indexes:
  failureModes:
    indexType: btree
    properties:
    - failureModes
    cursorable: false
usedFor: node