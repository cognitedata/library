externalId: "tr_{{location}}_pi_timeseries"
name: "tr:{{location}}:pi:timeseries"
dataSetExternalId: "{{dataset}}"
destination:
  type: instances
  dataModel:
    space: "{{schemaSpace}}"
    externalId: ISATimeSeries
    version: "{{dataModelVersion}}"
    destinationType: ISATimeSeries
  instanceSpace: "{{instanceSpace}}"
ignoreNullFields: true
isPublic: true
conflictMode: upsert
