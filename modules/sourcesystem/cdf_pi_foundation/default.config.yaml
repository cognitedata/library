# PI Foundation — configuration variables

# Site/location code, used in externalIds (e.g. ep_{{location}}_pi)
# Populated automatically from the site / location name entered in setup_project.py.
location: ""

# DM space where the extractor writes ExtractorTimeSeries instances (CDM destination)
instanceSpace: "sp_isa_instance_space"

# dataSetExternalId for the extraction pipeline
dataset: "ds_pi"

# External ID prefix for PI tag timeseries instances (used by the extractor)
piIdPrefix: "pi:"

# Integration owner (technical contact for the pipeline)
# Populated by setup_project.py wizard.
integration_owner_name: ""
integration_owner_email: ""

# Data owner (business contact for the data)
# Populated by setup_project.py wizard.
data_owner_name: ""
data_owner_email: ""
