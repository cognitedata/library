externalId: "tr_{{location}}_sap_maintenance_orders"
name: "tr:{{location}}:sap:maintenance_orders"
dataSetExternalId: "{{dataset}}"
destination:
  type: instances
  dataModel:
    space: "{{schemaSpace}}"
    externalId: WorkOrder
    version: "{{dataModelVersion}}"
    destinationType: WorkOrder
  instanceSpace: "{{instanceSpace}}"
ignoreNullFields: true
isPublic: true
conflictMode: upsert
