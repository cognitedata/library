externalId: "tr_{{location}}_sap_operations"
name: "tr:{{location}}:sap:operations"
dataSetExternalId: "{{dataset}}"
destination:
  type: instances
  dataModel:
    space: "{{schemaSpace}}"
    externalId: Operation
    version: "{{dataModelVersion}}"
    destinationType: Operation
  instanceSpace: "{{instanceSpace}}"
ignoreNullFields: true
isPublic: true
conflictMode: upsert
