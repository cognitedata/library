externalId: "tr_{{location}}_sap_equipment_to_asset"
name: "tr:{{location}}:sap:equipment_to_asset"
dataSetExternalId: "{{dataset}}"
destination:
  type: instances
  dataModel:
    space: "{{schemaSpace}}"
    externalId: Equipment
    version: "{{dataModelVersion}}"
    destinationType: Equipment
  instanceSpace: "{{instanceSpace}}"
ignoreNullFields: true
isPublic: true
conflictMode: upsert
