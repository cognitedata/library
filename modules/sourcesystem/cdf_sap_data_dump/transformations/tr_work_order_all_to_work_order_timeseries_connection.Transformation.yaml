externalId: tr_work_order_all_to_work_order_timeseries_connection
name: Work order to time series connection
destination:
  dataModel:
    space: '{{ space }}'
    externalId: '{{ data_model_external_id }}'
    version: '{{ dm_version }}'
    destinationType: WorkOrder
  instanceSpace: '{{ instance_space }}'
  type: instances
dataSetExternalId: '{{ dataset_external_id }}'
ignoreNullFields: true
isPublic: true
conflictMode: upsert
