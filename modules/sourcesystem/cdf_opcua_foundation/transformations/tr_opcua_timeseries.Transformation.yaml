externalId: "tr_{{location}}_opcua_timeseries"
name: "tr:{{location}}:opcua:timeseries"
dataSetExternalId: "{{dataset}}"
destination:
  type: instances
  dataModel:
    space: "{{schemaSpace}}"
    externalId: ISATimeSeries
    version: "{{dataModelVersion}}"
    destinationType: ISATimeSeries
  instanceSpace: "{{instanceSpace}}"
ignoreNullFields: true
isPublic: true
conflictMode: upsert
