---
description: Do not edit key_extraction_aliasing_jonluca; it is a read-only merge source
alwaysApply: true
---

# `key_extraction_aliasing_jonluca` is static

Treat **`modules/accelerators/contextualization/key_extraction_aliasing_jonluca/`** as **read-only** for all agent work.

- **Do not** change, refactor, or “sync” files under that tree unless the user explicitly asks to read or compare it for a merge decision.
- **Do** implement features, fixes, and documentation in **`cdf_key_extraction_aliasing`** (and shared paths outside `key_extraction_aliasing_jonluca` as directed).

Rationale: that directory is a temporary merge source and will be discarded when the merge is complete.
